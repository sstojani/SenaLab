# ── Never commit these ──────────────────────────────────────────
# Claude Code internal settings and skills — not part of the project
.claude/

# config.js holds local Supabase credentials.
# On Netlify, build.js injects them inline into index.html from env vars.
# Create your own local config.js — it is never committed to git.
config.js

# Node modules (for Netlify Functions dev)
node_modules/
netlify/functions/node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*.swo

# Local dev server log
*.log
